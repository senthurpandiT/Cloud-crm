export enum Roles {
  Admin = 'admin',
  User = 'user',
  SuperAdmin = 'SuperAdmin',
}

export enum languages {
  English = 'English',
  Danish = "Danish",
  Finnish = "Finnish",
  French = "French",
  Portuguese = "Portuguese",
}
